import { Image, StyleSheet, Platform } from 'react-native';

import HomveView from '@/src/views/HomeView';

export default function HomeScreen() {
  return (
    <HomveView />
  );
}

const styles = StyleSheet.create({
  titleContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
  stepContainer: {
    gap: 8,
    marginBottom: 8,
  },
  reactLogo: {
    height: 178,
    width: 290,
    bottom: 0,
    left: 0,
    position: 'absolute',
  },
});
